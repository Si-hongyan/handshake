# WSL 侧（状态机仿真 + 后续真机控制）
# 阶段一（仅仿真 + 单元测试）：无需第三方包，系统 python3 + 标准库 unittest 即可运行。
# 下面为“进入真机阶段(C/D/E)后”才需要的依赖，本阶段不安装。
PyYAML>=6.0
# --- 以下仅真机阶段使用，本阶段请勿安装 ---
# lerobot==0.6.0          # 以官方已确认兼容版本为准
# pyserial>=3.5
